class_name OfficePalette

# =============================================================================
# ENVIRONMENT - Walls, Floor, Structural Elements
# =============================================================================

# Floor
const FLOOR_CARPET = Color(0.55, 0.52, 0.48)
const FLOOR_LINE_LIGHT = Color(0.52, 0.49, 0.45)
const FLOOR_LINE_DARK = Color(0.50, 0.47, 0.43)

# Walls
const WALL_BEIGE = Color(0.85, 0.82, 0.78)
const WALL_SEAM_DARK = Color(0.2, 0.18, 0.15)

# Shadows (use with alpha)
const SHADOW = Color(0.0, 0.0, 0.0, 0.15)
const SHADOW_MEDIUM = Color(0.0, 0.0, 0.0, 0.2)

# =============================================================================
# FURNITURE - Wood and Metal Tones
# =============================================================================

# Wood (frames, doors)
const WOOD_FRAME = Color(0.45, 0.35, 0.28)
const WOOD_DOOR = Color(0.55, 0.42, 0.32)
const WOOD_DOOR_DARK = Color(0.45, 0.35, 0.28)
const WOOD_DOOR_LIGHT = Color(0.6, 0.48, 0.38)

# Desk
const DESK_SURFACE = Color(0.76, 0.70, 0.60)
const DESK_EDGE = Color(0.55, 0.50, 0.42)

# Meeting Table (darker wood conference table)
const MEETING_TABLE_SURFACE = Color(0.42, 0.32, 0.24)
const MEETING_TABLE_EDGE = Color(0.35, 0.26, 0.18)
const MEETING_TABLE_LEG = Color(0.30, 0.22, 0.16)

# Metal (filing cabinet, etc)
const METAL_GRAY = Color(0.5, 0.5, 0.52)
const METAL_GRAY_LIGHT = Color(0.55, 0.55, 0.58)
const METAL_GRAY_DARK = Color(0.45, 0.45, 0.48)
const METAL_HANDLE = Color(0.38, 0.38, 0.4)

# =============================================================================
# OFFICE EQUIPMENT - Computers, Shredder
# =============================================================================

# Monitor
const MONITOR_FRAME = Color(0.18, 0.18, 0.20)
const MONITOR_STAND = Color(0.22, 0.22, 0.25)
const MONITOR_SCREEN_OFF = Color(0.08, 0.08, 0.10)
const MONITOR_SCREEN_ON = Color(0.15, 0.25, 0.18)
const MONITOR_SCREEN_GLOW = Color(0.2, 0.35, 0.22)

# Status indicators
const STATUS_LED_RED = Color(0.8, 0.2, 0.2)
const STATUS_LED_GREEN = Color(0.2, 0.8, 0.2)
const STATUS_LED_GREEN_BRIGHT = Color(0.2, 0.85, 0.2)

# Keyboard/mouse
const KEYBOARD_DARK = Color(0.25, 0.25, 0.28)
const KEYBOARD_KEYS = Color(0.32, 0.32, 0.35)
const MOUSE_HIGHLIGHT = Color(0.35, 0.35, 0.38)

# Shredder
const SHREDDER_BODY = Color(0.22, 0.22, 0.25)
const SHREDDER_BODY_FRONT = Color(0.25, 0.25, 0.28)
const SHREDDER_TOP = Color(0.3, 0.3, 0.33)
const SHREDDER_BIN = Color(0.3, 0.3, 0.33)
const SHREDDER_BIN_FRONT = Color(0.35, 0.35, 0.38)
const SHREDDER_SLOT = Color(0.12, 0.12, 0.15)
const SHREDDED_PAPER = Color(0.92, 0.92, 0.88)
const SHREDDED_PAPER_DARK = Color(0.88, 0.88, 0.82)

# =============================================================================
# WATER COOLER
# =============================================================================

const COOLER_BASE = Color(0.75, 0.75, 0.78)
const COOLER_BASE_DARK = Color(0.65, 0.65, 0.68)
const COOLER_BODY = Color(0.85, 0.85, 0.88)
const COOLER_BODY_FRONT = Color(0.78, 0.78, 0.82)
const COOLER_BODY_TOP = Color(0.88, 0.88, 0.92)
const COOLER_BOTTLE = Color(0.6, 0.78, 0.9, 0.75)
const COOLER_BOTTLE_TOP = Color(0.7, 0.85, 0.95, 0.85)
const COOLER_TAP = Color(0.5, 0.5, 0.55)

# =============================================================================
# PLANT
# =============================================================================

const POT_TERRACOTTA = Color(0.6, 0.35, 0.25)
const POT_TERRACOTTA_DARK = Color(0.52, 0.3, 0.2)
const POT_RIM = Color(0.65, 0.4, 0.28)
const SOIL_DARK = Color(0.32, 0.25, 0.18)
const LEAF_GREEN = Color(0.3, 0.55, 0.3)
const LEAF_GREEN_LIGHT = Color(0.35, 0.6, 0.35)
const LEAF_GREEN_DARK = Color(0.28, 0.52, 0.28)

# =============================================================================
# WINDOWS - Sky and Outdoors
# =============================================================================

const SKY_BLUE = Color(0.5, 0.7, 0.95)
const CLOUD_WHITE = Color(0.95, 0.95, 0.98, 0.8)
const TREE_GREEN = Color(0.25, 0.5, 0.3)

# =============================================================================
# UI ELEMENTS
# =============================================================================

# Labels and tooltips
const UI_BG_DARK = Color(0.15, 0.15, 0.18, 0.9)
const UI_BG_DARKER = Color(0.2, 0.2, 0.22, 0.9)
const UI_TEXT_LIGHT = Color(0.95, 0.95, 0.95)
const UI_TEXT_DARK = Color(0.25, 0.25, 0.25)
const UI_TEXT_GRAY = Color(0.4, 0.4, 0.4)
const UI_TEXT_MUTED = Color(0.7, 0.7, 0.7)

# Tooltip
const TOOLTIP_BG = Color(0.95, 0.93, 0.85, 0.98)
const TOOLTIP_BORDER = Color(0.6, 0.55, 0.45)
const TOOLTIP_DIVIDER = Color(0.7, 0.65, 0.55)

# Speech bubble
const SPEECH_BUBBLE = Color(1.0, 1.0, 0.85, 0.95)
const SPEECH_BUBBLE_BORDER = Color(0.3, 0.3, 0.25)

# Exit sign
const EXIT_SIGN_BG = Color(0.8, 0.15, 0.15)
const EXIT_SIGN_TEXT = Color(0.95, 0.95, 0.95)

# Taskboard
const TASKBOARD_BG = Color(0.95, 0.93, 0.88)
const TASKBOARD_BORDER = Color(0.55, 0.45, 0.35)
const TASKBOARD_HEADER = Color(0.3, 0.3, 0.32)

# =============================================================================
# AGENT COLORS - Clothing by Type
# =============================================================================

const AGENT_SHIRT_WHITE = Color(0.95, 0.95, 0.93)
const AGENT_BLOUSE_PINK = Color(0.85, 0.75, 0.80)
const AGENT_BLOUSE_BLUE = Color(0.75, 0.85, 0.90)
const AGENT_BLOUSE_CREAM = Color(0.90, 0.88, 0.80)
const AGENT_TROUSERS_DARK = Color(0.25, 0.25, 0.28)
const AGENT_SKIRT_DARK = Color(0.25, 0.25, 0.35)

# Agent type accent colors (tie/necklace)
const AGENT_TYPE_ORCHESTRATOR = Color(0.85, 0.75, 0.55)  # Tan/khaki
const AGENT_TYPE_EXPLORE = Color(0.45, 0.55, 0.70)       # Dusty blue
const AGENT_TYPE_CODER = Color(0.50, 0.60, 0.50)         # Sage green
const AGENT_TYPE_DEBUGGER = Color(0.70, 0.45, 0.45)      # Dusty red
const AGENT_TYPE_REFACTOR = Color(0.60, 0.50, 0.65)      # Muted purple
const AGENT_TYPE_TEST = Color(0.70, 0.65, 0.45)          # Mustard
const AGENT_TYPE_RESEARCH = Color(0.50, 0.65, 0.65)      # Teal
const AGENT_TYPE_PLAN = Color(0.70, 0.55, 0.45)          # Terracotta
const AGENT_TYPE_REVIEWER = Color(0.55, 0.50, 0.60)      # Mauve
const AGENT_TYPE_DEFAULT = Color(0.55, 0.55, 0.55)       # Gray

# =============================================================================
# TOOL INDICATOR COLORS
# =============================================================================

const TOOL_BASH = Color(0.2, 0.8, 0.2)
const TOOL_READ = Color(0.4, 0.6, 1.0)
const TOOL_EDIT = Color(1.0, 0.8, 0.2)
const TOOL_WRITE = Color(1.0, 0.6, 0.2)
const TOOL_GLOB = Color(0.8, 0.4, 1.0)
const TOOL_GREP = Color(0.8, 0.4, 1.0)
const TOOL_WEB = Color(0.2, 0.8, 0.8)
const TOOL_TASK = Color(0.9, 0.5, 0.2)
const TOOL_DEFAULT = Color(0.5, 0.5, 0.5)

# =============================================================================
# CAT COLORS
# =============================================================================

const CAT_ORANGE_TABBY = Color(0.65, 0.45, 0.25)
const CAT_BLACK = Color(0.25, 0.25, 0.28)
const CAT_WHITE = Color(0.9, 0.88, 0.82)
const CAT_GRAY = Color(0.55, 0.55, 0.52)
const CAT_BROWN_TABBY = Color(0.4, 0.32, 0.25)
const CAT_CREAM = Color(0.8, 0.6, 0.35)
const CAT_EYES_GREEN = Color(0.3, 0.5, 0.3)
const CAT_INNER_EAR = Color(0.9, 0.7, 0.7)
const CAT_SLEEPING_Z = Color(0.6, 0.6, 0.7, 0.8)

# =============================================================================
# DOCUMENT/FOLDER
# =============================================================================

const MANILA_FOLDER = Color(0.85, 0.78, 0.55)
const PAPER_WHITE = Color(0.98, 0.98, 0.95)

# =============================================================================
# PERSONAL ITEMS
# =============================================================================

# Mug color options
const MUG_WHITE = Color(0.9, 0.9, 0.9)
const MUG_RED = Color(0.85, 0.3, 0.3)
const MUG_BLUE = Color(0.3, 0.5, 0.85)
const MUG_GREEN = Color(0.4, 0.7, 0.4)
const MUG_YELLOW = Color(0.95, 0.85, 0.4)

# Pencil cup
const PENCIL_CUP = Color(0.3, 0.3, 0.35)
const PENCIL_YELLOW = Color(0.9, 0.8, 0.2)
const PENCIL_BLUE = Color(0.2, 0.4, 0.8)

# Water bottle
const WATER_BOTTLE = Color(0.6, 0.8, 0.95, 0.8)
const WATER_BOTTLE_CAP = Color(0.3, 0.5, 0.8)

# Photo frame
const PHOTO_FRAME_WOOD = Color(0.4, 0.3, 0.25)
const PHOTO_SKY = Color(0.7, 0.8, 0.9)

# Figurine base
const FIGURINE_BASE = Color(0.3, 0.3, 0.3)
